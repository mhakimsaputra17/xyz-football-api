# Version control
.git
.gitignore

# Environment files (secrets — never bake into image)
.env
.env.*
!.env.example

# IDE / editor
.vscode
.idea
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Go build artifacts
/server
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test files and mocks (not needed in production image)
*_test.go
internal/mocks/
.mockery.yaml

# Docker files (no need to copy into build context)
Dockerfile
docker-compose.yml
docker-compose*.yml

# Documentation (markdown only — Swagger generated docs are needed)
*.md
LICENSE
